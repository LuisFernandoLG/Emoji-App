import styled from "styled-components";

const MainTitle = () => {
  return <MainTitleWrapper>Emoji App!</MainTitleWrapper>;
};

export default MainTitle;

const MainTitleWrapper = styled.h1`
  text-align: center;
`;
